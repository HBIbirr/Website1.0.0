{
  "build": {
    "builder": "NIXPACKS",
    "command": "npm install && npm run build"
  },
  "deploy": {
    "start_command": "npx vite preview --port 8080 --host",
    "output_directory": "dist"
  }
}